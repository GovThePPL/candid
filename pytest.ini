[pytest]
testpaths = backend/tests
markers =
    smoke: Quick sanity checks
    mutation: Tests that modify database state (use rollback pattern)
    integration: Tests that verify integration between services (API, Chat Server, Redis)
